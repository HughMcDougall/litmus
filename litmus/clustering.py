import numpy as np
from sklearn.cluster import DBSCAN
from litmus._utils import *


# ========================
def clusterfind_1D(X: [float]) -> [int]:
    '''
    For some array of samples 'X', associate them with separated clusters
    :param X:
    :return:
    '''
    # todo - fill this out
    return (np.ones_like(X))


def sort_by_cluster(samples: dict, indices: [int]):
    '''
    A utility to break a dictionary up by some clustering method
    :param samples:
    :param indices:
    :return:
    '''
    # todo - fill this out
    return samples
